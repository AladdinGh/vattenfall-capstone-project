# Milestone 6 — Design the SQL Layer

Inside `sql/`, create:
- `01_uc_setup.sql`
- `02_day1_bronze_validation.sql`

Optional:
- `03_permissions_preview.sql`

## Why this matters
Some logic belongs in notebooks.
Some logic belongs in reusable SQL artifacts.

Setup and validation SQL should be easy to find and easy to reuse.
